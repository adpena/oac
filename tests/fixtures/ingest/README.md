# Ingest fixtures

These fixtures are generated snapshots of `oac ingest` output for the projection fixtures.

Update them with:

```bash
python scripts/update_fixtures.py
```

They are contract tests for starter ingest behavior.
