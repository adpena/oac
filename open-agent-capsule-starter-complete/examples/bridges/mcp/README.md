# MCP bridge example

This directory holds a placeholder bridge sketch for exposing read-only capsule or hook state over MCP.

Replace the starter Python script with a real implementation using the official SDK in the language that best fits your deployment environment.
