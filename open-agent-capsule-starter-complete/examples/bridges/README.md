# Bridge examples

These examples clarify the role of MCP and WebMCP inside OAC.

- MCP and WebMCP are bridge surfaces, not canonical storage.
- The bridge exposes capabilities; the runtime still belongs to the adapter or hook.
- The launch posture is read-only.

See `docs/spec/003-target-mappings.md` for the target-level boundary.
