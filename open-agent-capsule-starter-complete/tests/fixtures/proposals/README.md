# Proposal fixtures

These JSON files are snapshot outputs from `oac propose` after running hydrate + ingest for each bundled target against `examples/hello-capsule/`.

Regenerate with:

```bash
python scripts/update_fixtures.py
```
