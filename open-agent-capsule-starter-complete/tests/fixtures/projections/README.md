# Projection fixtures

These fixtures are generated snapshots of `oac hydrate` output for the example capsule.

Update them with:

```bash
python scripts/update_fixtures.py
```

They are contract tests for starter hydrate behavior.
