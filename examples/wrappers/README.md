# Wrapper examples

Wrappers are launch helpers for real local harness installs.

Use them when a team needs to inject:

- local environment variables
- provider-specific flags
- model flags
- sandbox defaults

Do not use wrappers to change canonical meaning.
